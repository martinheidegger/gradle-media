apply plugin: 'groovy'

dependencies {
	compile gradleApi()
	groovy localGroovy()
}

def jars = fileTree(dir: 'lib', include: '**/*')

dependencies {
	runtime jars
}

sourceSets {
	main {
		compileClasspath += jars
	}
}